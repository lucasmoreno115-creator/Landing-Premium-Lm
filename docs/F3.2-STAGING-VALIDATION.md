# F3.2 — Conversion Staging Validation

## Status

**STOP — staging environment not available for authoritative browser validation.**

## Authoritative baseline

- Branch: `lm-2.0-rebuild`
- Baseline commit inspected: `149f0997436e77157c71e5bdfbe55b79064c8fdd`
- Conversion implementation files present:
  - `index.html`
  - `scripts/analytics.js`
  - `scripts/conversion.js`
  - `styles/conversion.css`
  - `docs/F3.1-CONVERSION-QA.md`

## Preflight performed

### PASS — branch/content identity

The required F3.1 implementation exists on `lm-2.0-rebuild` and the branch points to the expected baseline commit.

### PASS — static conversion wiring

Static inspection confirms:

- Home loads `scripts/analytics.js` before `scripts/conversion.js`, both with `defer`.
- Qualification uses a native `<dialog>`.
- Premium, audience and final CTAs open the qualification flow.
- Qualification has 3 questions and a fourth completion state.
- Premium and training routes generate contextual WhatsApp URLs.
- WhatsApp target is `5514991174500`.
- Conversion events are delegated to `window.LMAnalytics`.
- UTM attribution and `lm_session_id` are session-scoped.
- Storage access is guarded so restricted `sessionStorage` does not break the flow.

### STOP — deployed staging URL

No staging URL, staging-specific deployment configuration, or deployment workflow is present in the inspected branch tree. The repository contains a `CNAME` for `lucasmorenopersonal.com.br`, which is the production custom domain, not an isolated staging target.

The branch tree also contains no `.github` deployment workflow or other staging configuration that would establish an independently addressable browser environment for `lm-2.0-rebuild`.

Because F3.2 is explicitly a **staging browser validation**, production cannot be used as a substitute and the release gate must not be marked PASS from static source inspection alone.

## Browser matrix pending

The following checks remain **NOT EXECUTED** until a staging URL serves the exact baseline under validation:

| Area | Required validation |
| --- | --- |
| 360 px mobile | Layout, dialog fit, scrolling, choices, CTA visibility |
| 390–430 px mobile | Same as above |
| Tablet | Modal sizing, focus, content flow |
| Desktop ≥ 1024 px | Header, modal, keyboard and conversion path |
| Keyboard-only | Tab order, modal focus trap, return focus |
| Escape | `close_reason=escape` |
| Backdrop | `close_reason=backdrop` |
| Close button | `close_reason=close_button` |
| Q1 → Q2 → Q3 | Progress and answer transitions |
| Back | Earlier answer retained until overwritten |
| Restart | State cleared and Q1 restored |
| Premium route | Correct summary, route and WhatsApp text |
| Training route | Correct summary, route and WhatsApp text |
| Unknown-format route | Premium conversation path without overclaiming fit |
| WhatsApp encoding | Accents/newlines encoded and decoded correctly |
| `page_view` | Present once on load |
| Funnel events | Expected order and source metadata |
| UTM entry | Attribution appears in `window.dataLayer` |
| UTM continuity | Attribution survives refresh/navigation in same session |
| Restricted storage | Page and conversion flow continue working |
| Console | No uncaught JS errors or missing static assets |

## Required condition to resume

Provide or create an isolated staging deployment that serves **exactly** the `lm-2.0-rebuild` commit intended for validation. Record both:

1. `STAGING_URL`
2. `STAGING_COMMIT_SHA`

Before executing the matrix, verify that the deployed source corresponds to that commit. If the branch advances after deployment, redeploy and update `STAGING_COMMIT_SHA` before validating.

## Release rule

F3.2 may be marked **PASS** only after the browser matrix is executed against the isolated staging deployment and all critical conversion paths, WhatsApp routing, accessibility behaviors, analytics events and UTM continuity pass without blocking defects.
